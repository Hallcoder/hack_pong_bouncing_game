class Main{
    function void main(){
        var Ball ball;
        var int x;
        var int ballPosition;
        var int line_position;
        var Line_board line;
        let line = Line_board.new(200,250,16);
        let ball = Ball.new(256,15,9);
        do ball.draw();
        do line.draw();
        let x = 0;
        do ball.moveDown();
        
        
        return;
    }
}